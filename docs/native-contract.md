# Native contract

Moved to the docs site, so the contract lives in one place instead of two
copies that drift:

**<https://camera.vyui.dev/docs/native-contract>** — prop and option defaults,
result shapes, event payloads, session controls, and recording.

**<https://camera.vyui.dev/docs/errors>** — the error-code table, which now has
a page of its own.

Source: `website/content/docs/native-contract.mdx` and
`website/content/docs/errors.mdx`. Edit those, not this file.
