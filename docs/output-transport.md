# Output transport

Moved to the docs site, so the transport rules live in one place instead of two
copies that drift:

**<https://camera.vyui.dev/docs/output-transport>** — why a capture returns a
path rather than base64, how to display and upload one, saving to the gallery,
and who owns the temp file's lifetime.

Source: `website/content/docs/output-transport.mdx`. Edit that, not this file.
