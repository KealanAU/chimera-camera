# Mock testing

Moved to the docs site, so the mock's behaviour is documented in one place
instead of two copies that drift:

**<https://camera.vyui.dev/docs/mock>** — basic usage, what the default mock
returns, custom fixtures, and guarding production against it.

Source: `website/content/docs/mock.mdx`. Edit that, not this file.
