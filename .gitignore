node_modules/
dist/

# Local reference clone of react-native-vision-camera (has its own .git).
# Re-clone it if missing; see docs/archive/V1.md "Upstream Reference Strategy".
upstream/

.DS_Store
*.tgz
.env*

# Android/Gradle build output. Xcode's lands in example/host-ios, which has
# its own .gitignore, as does website/.
android/build/
.gradle/
.cxx/
local.properties
